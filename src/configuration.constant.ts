export const configurationKeys = {
  in_production: 'in_production',
  downloadable_files_directory: 'downloadable_files_directory',
  // Temporarily store a prefix for all Angular component.
  // Later, we could allow the user to create a configuration for each project
  angular_selector_prefix: 'angular_selector_prefix',
  api_client_folder_path: 'api_client_folder_path',
  allow_origin: 'allow_origin',
  exposed_headers: 'exposed_headers'
};
